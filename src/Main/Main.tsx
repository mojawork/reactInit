import React, { Component } from 'react';
import './Main.scoped.scss';

class Main extends Component {
    render() {
        return (
            <main>
                Main
            </main>
        );
    }
}

export default Main;
